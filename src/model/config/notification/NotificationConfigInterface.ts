/**
 * Configuration of the notification system to configure stuff
 * like timeout.
 *
 * @since 2.0.0
 */
export default interface NotificationConfigInterface {
    /**
     * The timeout how long a single notification is being shown
     */
    timeout: number;
}
